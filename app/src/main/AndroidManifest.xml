<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Erforderlich, um auf die Glyph Matrix zugreifen zu koennen -->
    <uses-permission android:name="com.nothing.ketchum.permission.ENABLE" />

    <!-- Fuer die Termin-Erinnerungen -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".GlyphKalenderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- Test-Key aus dem offiziellen Nothing SDK-Demoprojekt. Fuer einen Release
             muss ein eigener Key ueber das Nothing Developer Programme beantragt werden. -->
        <meta-data
            android:name="NothingKey"
            android:value="test" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Unser erster Glyph Toy: zeigt die Uhrzeit, wird schrittweise um
             Termin-Animationen und Gestenerkennung erweitert -->
        <service
            android:name=".glyph.ClockGlyphToyService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.nothing.glyph.TOY" />
            </intent-filter>

            <meta-data
                android:name="com.nothing.glyph.toy.name"
                android:resource="@string/toy_name_clock" />
            <meta-data
                android:name="com.nothing.glyph.toy.image"
                android:resource="@mipmap/ic_launcher" />
            <meta-data
                android:name="com.nothing.glyph.toy.summary"
                android:resource="@string/toy_summary_clock" />
            <meta-data
                android:name="com.nothing.glyph.toy.longpress"
                android:value="1" />
        </service>

        <!-- Kurzlebiger Service, der unabhaengig vom aktuell ausgewaehlten Glyph Toy
             zur Terminzeit eine Animation auf der Matrix abspielt -->
        <service
            android:name=".glyph.ReminderGlyphService"
            android:exported="false"
            android:foregroundServiceType="shortService" />

        <receiver
            android:name=".reminder.ReminderAlarmReceiver"
            android:exported="false" />

        <!-- Siehe app-sim/AndroidManifest.xml und BootReceiver.kt zur Begruendung der
             einzelnen Ereignisse. -->
        <receiver
            android:name=".reminder.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
